import React from "react";
import Dashboard from "./dashboard";

// Wrapper component for the existing client dashboard
const ClientDashboard = () => {
    return <Dashboard />;
};

export default ClientDashboard;
